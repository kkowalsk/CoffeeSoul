> create reusable bar chart that shows the latest net balance for each person. neg to 0 to pos on the y-axis. insert it at the end of the OrdersView.js and at the top of the MetricsView.js

> analyze the markdown files under the prompts/ directory, and classify/count the prompts per the table listed in prompts.MD. Update the table counts

> instructions for how to deploy this project to EC2 instance. 
  Already have performed:
  0) ec2 instance created in AWS
  1) AWS cli installed and logged in
  2) one registry was created - should multiple be created, one per image?
    > ~20 additional back and forth prompts 